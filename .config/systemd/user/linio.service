[Unit]
Description=Linio
After=network.target

[Service]
ExecStart=%h/.local/bin/linio --home="/ToDo" --git %h/scribbles
Restart=always
WorkingDirectory=%h/scribbles
Environment=PATH=%h/.local/bin:/usr/local/bin:/usr/bin

[Install]
WantedBy=default.target
