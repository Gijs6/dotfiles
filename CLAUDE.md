# Claude Code Instructions

## Sudo Commands

**IMPORTANT**: When running sudo commands, ALWAYS use the `-A` flag to trigger the graphical password prompt.

- Correct: `sudo -A pacman -Syu`
- Wrong: `sudo pacman -Syu`
